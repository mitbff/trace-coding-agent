# 订单折扣演示项目

这是一个为三轮连续演示准备的小型项目。初始实现把折扣金额加到了价格上，因此两个测试失败；
零折扣测试通过。演示过程不需要预先告诉 Agent 错误位置。

建议依次输入：

```text
检查项目并运行测试，定位订单折扣计算错误，先不要修改代码。
```

```text
根据刚才的诊断修复折扣计算，并运行完整测试。
```

```text
继续为折扣计算增加非法折扣率校验：折扣率必须位于 0 到 1 之间。补充测试并运行完整测试。
```

完整测试命令：

```powershell
python -m pytest -q pricing_check.py order_check.py
```
